# CreaTech code repository

Read README.md and inspect git status before editing. Preserve the existing application and public-release approval checks. Keep draft data and credentials out of commits, build output, logs and artifacts. Use synthetic fixtures for tests. Do not invent programme content or participant consent. Report only checks actually run. Production releases and real invitations require organiser authorization.
