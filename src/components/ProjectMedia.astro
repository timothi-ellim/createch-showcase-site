---
import type { PublicProject } from '../data/catalogue';
import { responsiveMedia } from '../lib/responsive-media';
interface Props {
  project: PublicProject;
}
const { project } = Astro.props;
---

<figure class="project-figure">
  <div class:list={['project-media', project.theme]}>
    {project.media ? (
      <img
        src={project.media.src}
        srcset={responsiveMedia(project.media.src)}
        sizes="(max-width: 767px) 100vw, (max-width: 1023px) 50vw, 45vw"
        alt={project.media.alt}
        loading="lazy"
        width="960"
        height="640"
      />
    ) : (
      <>
        <span class="placeholder-symbol" aria-hidden="true">
          {project.theme === 'image'
            ? '◉'
            : project.theme === 'world'
              ? '↗'
              : '⤫'}
        </span>
        <span class="placeholder-caption">Image pending approval</span>
      </>
    )}
  </div>
  {project.media && <figcaption>{project.media.credit}</figcaption>}
</figure>
